const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'public', 'migrations', 'migrationsConfig', 'migrationsConfig.js'),
    'migrations-path': path.resolve(__dirname, 'public', 'migrations')
}